const {
    EmbedBuilder
} = require('discord.js')
const fs = require('fs')
module.exports = {
    name: 'checkNotifications',
    async execute(client) {
        return console.log("Check notifications event")
    },
};